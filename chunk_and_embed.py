# chunk_and_embed.py
# Chunks parsed document text into segments with rich metadata
# (source file name, page number, row number) and stores them
# in a ChromaDB vector collection for semantic retrieval.
